{
  "name": "mongobatch-js",
  "version": "0.2.0",
  "description": "process mongodb collections in batches",
  "license": "Apache-2.0",
  "main": "index.js",
  "author": {
    "name": "Andras",
    "url": "http://github.com/andrasq"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/andrasq/node-mongobatch-js"
  },
  "engines": {
    "node": ">=0.10"
  },
  "scripts": {
    "test": "nodeunit"
  },
  "keywords": [
    "Andras",
    "mongodb",
    "collection",
    "fast",
    "quick",
    "access"
  ],
  "dependencies": {
  },
  "devDependencies": {
    "mongodb": "*",
    "qnit": "0.18.0"
  },
  "scripts": {
    "test": "qnit test",
    "coverage": "nyc --include mongobatch.js --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output coverage"
  }
}
